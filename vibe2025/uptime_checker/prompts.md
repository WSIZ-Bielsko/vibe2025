
## prompts used for this project
```
create python 3.12,poetry, pydantic application structure, and implementation that 
would supervise availability of hosts and services; the app should be configured with 
a list of tuples (IP_or_domain, port), and should attemt to connect to these ports; 
the results should be dumped to a json file; the initial list of 
services should also be pulled from a json file; 
the frequency of availability checks should be configurable
```

```
create a simple browser.py that would ask for service / IP and port, and from/to datetimes 
and compute the uptime for the said service in the given period
```

